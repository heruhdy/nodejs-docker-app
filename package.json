{
  "name": "nodejs-docker-app",
  "version": "1.0.0",
  "description": "Simple Node.js application running with Docker",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
